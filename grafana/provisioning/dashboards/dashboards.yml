apiVersion: 1

providers:
  - name: "aws-infra-dashboards"
    orgId: 1
    folder: "AWS Infrastructure"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
